# Stub Makefile for amd-udi

all-local:
	@echo "TODO: find out what is supposed to be going on here."

CLEANFILES = *.o *~

DISTCLEANFILES = Makefile config.status stamp-h1 config.h *.log

SUBDIRS = mondfe montip udi
