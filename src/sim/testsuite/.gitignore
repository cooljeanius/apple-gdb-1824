*.status
*~
core
*.o
a.out
xgdb
*.x
*.grt
*.rum
*.c
*.dif
*.*out
Makefile

# gitignore subdirectories with their own configure scripts
# (until that I can verify that each of them works)
d10v-elf/*
d30v-elf/*
fr30-elf/*
frv-elf/*
m32r-elf/*

# gitignore other subdirectories that are so big that I will have
# to commit them separately later:
sim/*
